name: frontend

services:
  frontend:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    restart: on-failure
    networks:
      - caddy
    labels:
      caddy: localhost
      caddy.reverse_proxy: '{{upstreams 3001}}'

networks:
  caddy:
    external: true
